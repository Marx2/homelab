# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: cluster-secrets
    namespace: flux-system
stringData:
    SECRET_DOMAIN: ${BOOTSTRAP_DOMAIN}
    SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}

    SECRET_CLOUDFLARE_API_KEY: ${BOOTSTRAP_CLOUDFLARE_API_KEY}
    SECRET_CLOUDFLARE_TOKEN: ${BOOTSTRAP_CLOUDFLARE_TOKEN}
    SECRET_CLOUDFLARE_ZONE_ID: ${BOOTSTRAP_CLOUDFLARE_ZONE_ID}
    SECRET_YOUTUBE_TOKEN: ${BOOTSTRAP_YOUTUBE_TOKEN}
    SECRET_DEX_GITHUB_CLIENT_ID: ${BOOTSTRAP_DEX_GITHUB_CLIENT_ID}
    SECRET_DEX_GITHUB_CLIENT_SECRET: ${BOOTSTRAP_DEX_GITHUB_CLIENT_SECRET}
    SECRET_OAUTH2_PROXY_SECRET: ${BOOTSTRAP_OAUTH2_PROXY_SECRET}
    SECRET_OAUTH2_PROXY_COOKIE_SECRET: ${BOOTSTRAP_OAUTH2_PROXY_COOKIE_SECRET}
    SECRET_GENERIC_OAUTH_SECRET: ${BOOTSTRAP_GENERIC_OAUTH_SECRET}
    SECRET_DEX_GOOGLE_CLIENT_ID: ${BOOTSTRAP_DEX_GOOGLE_CLIENT_ID}
    SECRET_DEX_GOOGLE_CLIENT_SECRET: ${BOOTSTRAP_DEX_GOOGLE_CLIENT_SECRET}
    SECRET_WIREGUARD_PRIVATE_KEY: ${BOOTSTRAP_WIREGUARD_PRIVATE_KEY}
    SECRET_WIREGUARD_ADDRESS: ${BOOTSTRAP_WIREGUARD_ADDRESS}
    SECRET_WIREGUARD_DNS: ${BOOTSTRAP_WIREGUARD_DNS}
    SECRET_WIREGUARD_PUBLIC_KEY: ${BOOTSTRAP_WIREGUARD_PUBLIC_KEY}
    SECRET_WIREGUARD_ENDPOINT: ${BOOTSTRAP_WIREGUARD_ENDPOINT}
