---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-config-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: longhorn
