---
apiVersion: v1
kind: Secret
metadata:
  name: s3-secret
  namespace: database
stringData:
  s3.conf: |
    [global]
    repo1-s3-key=${MINIO_S3_ACCESS_KEY}
    repo1-s3-key-secret=$ś{MINIO_S3_SECRET_KEY}
