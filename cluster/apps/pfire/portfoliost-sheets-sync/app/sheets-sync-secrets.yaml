---
apiVersion: v1
kind: Secret
metadata:
  name: sheets-sync-secrets
  namespace: pfire
type: Opaque
stringData:
  # Service-account key JSON (single line, newlines stripped) for the
  # sheets-sync@xenon-point-416122 SA. Auth model + rotation:
  # portfoliost-sheets-sync/docs/GOOGLE-AUTH.md
  GOOGLE_SERVICE_ACCOUNT_JSON: ${PORTFOLIOST_SHEETS_SYNC_SA_JSON}
