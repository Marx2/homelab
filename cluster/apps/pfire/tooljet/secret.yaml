---
apiVersion: v1
kind: Secret
metadata:
  name: server
  namespace: pfire
type: Opaque
data:
  pg_host: "postgresql:5432"
  pg_db: tooljet
  pg_user: postgres
  pg_password: ${SECRET_POSTGRESQL_PASS}
  secret_key_base: ${TOOLJET_LOCKBOX_MASTER_KEY}
  lockbox_key: ${TOOLJET_SECRET_KEY_BASE}
