---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app portfoliost
  namespace: pfire
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  uninstall:
    keepHistory: false
  driftDetection:
    mode: enabled
    ignore:
      - paths:
          - /spec/containers/resources/limits
        target:
          kind: Pod
  values:
    global:
      createDefaultServiceAccount: false
    defaultPodOptions:
      imagePullSecrets:
        - name: regcred
    controllers:
      portfoliost:
        replicas: 1
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: ghcr.io/marx2/portfoliost
              tag: v1.0.60
            env:
              OIDC_ISSUER_URL: "https://pocket-id.${SECRET_DOMAIN}"
              OIDC_CLIENT_ID: "${PORTFOLIOST_OIDC_CLIENT_ID}"
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                memory: 64Mi
    service:
      app:
        controller: *app
        ports:
          http:
            port: &port 80
    ingress:
      app:
        enabled: true
        className: nginx
        annotations:
          hajimari.io/enable: "true"
          hajimari.io/appName: "Portfoliost"
          hajimari.io/icon: mdi:briefcase-outline
          hajimari.io/group: "pfire"
          hajimari.io/info: "Investment portfolio tracker"
        hosts:
          - host: &host "{{ .Release.Name }}.${SECRET_DOMAIN}"
            paths:
              - path: /
                service:
                  name: *app
                  port: *port
              - path: /api/auth
                service:
                  name: portfoliost-users-auth
                  port: 3000
              - path: /api/users
                service:
                  name: portfoliost-users-auth
                  port: 3000
              - path: /api/instruments
                service:
                  name: portfoliost-instruments
                  port: 3000
              - path: /api/logos
                service:
                  name: portfoliost-instruments
                  port: 3000
              - path: /api/wallets
                service:
                  name: portfoliost-wallets
                  port: 3000
              - path: /api/currencies
                service:
                  name: portfoliost-wallets
                  port: 3000
              - path: /api/fx
                service:
                  name: portfoliost-wallets
                  port: 3000
              - path: /api/admin
                service:
                  name: portfoliost-admin
                  port: 3000
              - path: /api
                service:
                  name: stoxly-mock
                  port: 4010
        tls:
          - hosts:
              - *host
