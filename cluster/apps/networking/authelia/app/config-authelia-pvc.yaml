---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-authelia-pvc
  namespace: networking
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
