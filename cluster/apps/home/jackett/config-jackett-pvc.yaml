---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-jackett-pvc
  namespace: home
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
