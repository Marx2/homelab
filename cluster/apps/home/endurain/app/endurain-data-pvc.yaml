apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: endurain-data-pvc
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
