---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app tvheadend
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 4.6.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  values:
    defaultPodOptions:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"
    #        supplementalGroups:
    #          - 44
    #          - 212
    #          - 109

    controllers:
      tvheadend:
        containers:
          tvheadend:
            image:
              repository: registry.skysolutions.fi/library/tvheadend
              tag: 4.3.10483@sha256:2d25eca4700691a52ab046d8df7fe0d0c31dca793b290bed1d841f0c8e23eb51
            command: "/entrypoint.sh"
            args: ["--noacl"]
            env:
              TZ: "${TZ}"
    #            securityContext:
    #              privileged: true
    # RUN_OPTS: "-d -l --debug -l --subsystems -l --trace -l --libav -l --uidebug"
          epg-updater:
            image:
              repository: alpine
              tag: latest
            command: ["/bin/sh", "-c"]
            args:
              - |
                apk add --no-cache socat wget && \
                wget -q -O - http://epg.ovh/pl.xml | socat - UNIX-CONNECT:/config/epggrab/xmltv.sock && \
                echo '0 */6 * * * wget -q -O - http://epg.ovh/pl.xml | socat - UNIX-CONNECT:/config/epggrab/xmltv.sock' > /etc/crontabs/root && \
                crond -f -d 8
            securityContext:
              runAsUser: 0
              runAsGroup: 0

    service:
      tvheadend:
        controller: tvheadend
        type: LoadBalancer
        externalIPs:
          - 192.168.1.211
        ports:
          http:
            enabled: true
            port: 80
            targetPort: 9981
          htsp:
            enabled: true
            protocol: TCP
            port: 9982

    ingress:
      app:
        enabled: true
        className: nginx
        annotations:
          #          nginx.ingress.kubernetes.io/auth-url: "https://auth.${SECRET_DOMAIN}/oauth2/auth"
          #          nginx.ingress.kubernetes.io/auth-signin: https://auth.${SECRET_DOMAIN}/oauth2/start
          #          nginx.ingress.kubernetes.io/proxy-body-size: 50m
          #          nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
          hajimari.io/enable: "true"
          hajimari.io/icon: "home-lightning-bolt-outline"
        hosts:
          - host: &host "{{ .Release.Name }}.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: tvheadend
                  port: http
        tls:
          - hosts:
              - *host

    persistence:
      config:
        enabled: true
        existingClaim: config-tvheadend-pvc
        globalMounts:
          - path: /config
      recordings-omv:
        enabled: true
        existingClaim: nfs-recordings-omv-pvc
        globalMounts:
          - path: /recordings
      timeshift:
        enabled: true
        type: emptyDir
        globalMounts:
          - path: /timeshift
    resources:
      requests:
        cpu: 100m
        memory: 20Mi
      limits:
        cpu: 1000m
        memory: 1096Mi

