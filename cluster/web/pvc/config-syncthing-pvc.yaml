---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-syncthing-pvc
  namespace: web
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
