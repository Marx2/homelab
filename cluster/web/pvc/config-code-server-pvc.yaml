---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-code-server-pvc
  namespace: web
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 300Mi
