---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radarr
  namespace: downloads
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: radarr
      version: 10.0.1
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: system-flux
      interval: 5m
  values:
    image:
      repository: ghcr.io/k8s-at-home/radarr
      tag: v3.0.2.4552
      pullPolicy: Always

    controllerType: deployment

    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000

    env:
      TZ: "Europe/Warsaw"

    persistence:
      config:
        enabled: true
        existingClaim: config-radarr-pvc
      media:
        enabled: true
        existingClaim: nfs-media-omv-pvc
        mountPath: /data/nas-media

    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: external
        nginx.ingress.kubernetes.io/auth-url: "https://auth.k.marx.katowice.pl/oauth2/auth"
        nginx.ingress.kubernetes.io/auth-signin: https://auth.k.marx.katowice.pl/oauth2/start
        forecastle.stakater.com/expose: "true"
        forecastle.stakater.com/icon: "https://sasquatters.com/media/2020/04/radarr-banner.jpg"
        forecastle.stakater.com/network-restricted: "true"
      hosts:
      - host: radarr.k.marx.katowice.pl
        paths:
        - path: /
          pathType: Prefix
      tls:
      - hosts:
        - "radarr.k.marx.katowice.pl"

    resources:
      requests:
        cpu: "0.04"
        memory: "256M"
      limits:
        memory: "512M"
